# Real (secret-bearing) config and runtime data — never commit.
# (paths anchored with / so they don't accidentally match nested dirs like cmd/csat)
/config.toml
/.env
/csat.env
*.db
*.db-wal
*.db-shm
/crypto.key

# Local runtime + per-customer bundles (contain secrets/branding)
/data/
/customers/
/logo.*

# Build output (anchored to the repo root — must NOT match cmd/csat)
/dist/
/csat

# OS / editor
.DS_Store
