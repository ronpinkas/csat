# Reverse-proxy vhost for CSAT (Apache) — serves https://csat.example.com from
# the local csat service on 127.0.0.1:8080.
# Requires: a2enmod proxy proxy_http headers ssl   (then `certbot --apache`)

<VirtualHost *:80>
    ServerName csat.example.com
    Redirect permanent / https://csat.example.com/
</VirtualHost>

<VirtualHost *:443>
    ServerName csat.example.com

    SSLEngine on
    SSLCertificateFile      /etc/letsencrypt/live/csat.example.com/fullchain.pem
    SSLCertificateKeyFile   /etc/letsencrypt/live/csat.example.com/privkey.pem

    ProxyPreserveHost On
    RequestHeader set X-Forwarded-Proto "https"
    ProxyPass        / http://127.0.0.1:8080/
    ProxyPassReverse / http://127.0.0.1:8080/
</VirtualHost>
