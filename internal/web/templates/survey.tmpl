{{define "title"}}{{.SiteName}}{{end}}
{{define "content"}}
<section class="card survey">
  {{if .LogoURL}}<img class="logo" src="{{.LogoURL}}" alt="{{.SiteName}}">{{end}}
  <h1>{{.SiteName}}</h1>
  {{if .Intro}}<p class="lede">{{.Intro}}</p>{{end}}

  <form method="POST" action="/s?t={{.Token}}">
    <input type="hidden" name="csrf" value="{{.CSRF}}">
    <input type="hidden" name="set" value="{{.SetID}}">

    {{range $q := .Questions}}
    <fieldset class="field">
      <legend>{{$q.Label}}</legend>

      {{if eq $q.Type "stars"}}
      <div class="stars" role="radiogroup" aria-label="{{$q.Label}}">
        {{range $v := $q.Stars}}
        <label class="star">
          <input type="radio" name="{{$q.Key}}" value="{{$v}}" {{if $q.Required}}required{{end}}>
          <span aria-hidden="true">★</span>
          <span class="sr-only">{{$v}}</span>
        </label>
        {{end}}
      </div>

      {{else if or (eq $q.Type "scale") (eq $q.Type "nps")}}
      <div class="scale" role="radiogroup" aria-label="{{$q.Label}}">
        {{range $v := $q.Scale}}
        <label class="scale-pt">
          <input type="radio" name="{{$q.Key}}" value="{{$v}}" {{if $q.Required}}required{{end}}>
          <span>{{$v}}</span>
        </label>
        {{end}}
      </div>
      {{if or $q.EndLow $q.EndHigh}}<div class="scale-ends"><span>{{$q.EndLow}}</span><span>{{$q.EndHigh}}</span></div>{{end}}

      {{else if eq $q.Type "choice"}}
      <div class="choices">
        {{range $o := $q.Options}}
        <label class="choice"><input type="radio" name="{{$q.Key}}" value="{{$o.Value}}" {{if $q.Required}}required{{end}}> {{$o.Label}}</label>
        {{end}}
      </div>

      {{else if eq $q.Type "multichoice"}}
      <div class="choices">
        {{range $o := $q.Options}}
        <label class="choice"><input type="checkbox" name="{{$q.Key}}" value="{{$o.Value}}"> {{$o.Label}}</label>
        {{end}}
      </div>

      {{else if eq $q.Type "text"}}
      <textarea name="{{$q.Key}}" rows="3" {{if $q.MaxLen}}maxlength="{{$q.MaxLen}}"{{end}} placeholder="{{$q.Placeholder}}" {{if $q.Required}}required{{end}}></textarea>
      {{end}}
    </fieldset>
    {{end}}

    <button type="submit" class="btn primary">{{.Submit}}</button>
  </form>
</section>
{{end}}
